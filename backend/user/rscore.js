const getUserReliabilityScore = async (req, res) => {
    try{
        // query ratings from public POIs created by user

        // query number of verified POIs reported by user

        // need some algorithm to weight contributions
    }catch(err){

    }
}

export const rscore = getUserReliabilityScore;